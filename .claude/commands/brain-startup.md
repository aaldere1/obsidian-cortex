---
description: Start a work session — pull the vault, show what's active across machines, brief me, and mark this machine active.
---
Invoke the `brain-startup` skill. Detect this machine's name from its folder under `AI Brain/Machines/`, pull the vault, run a snapshot of activity across all machines, give me a short brief on where things stand, and write Current Activity for this machine.

# Projects

Each project gets its own folder, created from `_Template Project` via:

```bash
node "AI Brain/scripts/brain.mjs" init-project "My Project"
```

It contains: Overview, Current State, Decisions, Next Steps, and a Sessions/ log.
Delete this example file once you have real projects.

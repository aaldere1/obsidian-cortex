# Fleet Apply Queue

Purpose: Learnings that every machine/agent should **pull and apply** — not just read. Bidirectional: any seat that learns something fleet-useful writes it here (or links a Shared decision/lock) and pushes; other machines apply it on their next startup.

Last updated: {{DATE}}

## How to use (every machine)

1. `git pull --ff-only` in the vault (brain-startup does this).
2. Read this file + any linked notes.
3. Apply Open items that touch this machine (template changes, habits, local skill copies).
4. Record the machine name + date under **Applied on**.
5. If *you* learned something fleet-useful: add an Open item here (or a Shared note + link) and push — don't keep it local-only.

## Open — apply on next load

### FA-YYYY-MM-DD-a — Short title
- **Source:** [[Link to the decision or lock note]]
- **Do:**
  1. Concrete change each machine should make
- **Applied on:**
  - <MACHINE> — YYYY-MM-DD

## Done — applied everywhere

- FA-YYYY-MM-DD-x — Short title (closed YYYY-MM-DD)
